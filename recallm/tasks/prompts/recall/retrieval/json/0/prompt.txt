Your task is to retrieve a value from a JSON dictionary.

You are given:
1) A JSON object called `retrieval_json` that maps integer keys (as strings) to values.
2) A key to look up.

Steps:
- Find the specified key in `retrieval_json`.
- Return the corresponding value exactly as it appears.

Do your reasoning step-by-step. On the final line, output only:
Answer: <the value>

{question_start}retrieval_json:
```json
{documents}
```

{question_end}