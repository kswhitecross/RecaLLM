Your task is to retrieve a value from a CSV table.

You are given:
1) A CSV table with columns `key` and `value`.
2) A key to look up.

Steps:
- Find the row matching the specified key.
- Return the corresponding value exactly as it appears.

Do your reasoning step-by-step. On the final line, output only:
Answer: <the value>

{question_start}```csv
{documents}
```

{question_end}