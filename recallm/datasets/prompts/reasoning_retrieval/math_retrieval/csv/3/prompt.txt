Your task is to retrieve a value from a large lookup table called `data_table`. The key you need to find the correct row is given to you as a math problem. Solve that math problem, then look up the row with that key in the table.

Do your reasoning step-by-step. On the final line, output only:
Answer: <the value>

{question_start}data_table:
```csv
{documents}
```

Now, compute the answer to the math problem, and then output the value from the row in `data_table` where the key matches your answer.

{question_end}